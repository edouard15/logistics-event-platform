service:
  type: ClusterIP

config:
  outputs: |
    [OUTPUT]
        Name  es
        Match *
        Host  elasticsearch-master
        Port  9200
        Index kubernetes-logs
        Logstash_Format On
