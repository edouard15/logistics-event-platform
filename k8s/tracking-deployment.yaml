apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      containers:
      - name: tracking-service
        image: edouard15/logistics-tracking:latest
        ports:
        - containerPort: 8000
